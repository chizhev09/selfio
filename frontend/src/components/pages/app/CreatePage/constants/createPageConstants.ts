// Числовые константы карусели и демо-баланса на экране «Создать».

/** Демо-баланс монет в модалке (как на экране шаблона в библиотеке). */
export const CREATE_PAGE_COIN_BALANCE = 1240

/** Внутреннее кольцо карусели: число спиц. */
export const CREATE_ARC_INNER_COUNT = 10

/** Внешнее кольцо карусели: число спиц. */
export const CREATE_ARC_OUTER_COUNT = 20
